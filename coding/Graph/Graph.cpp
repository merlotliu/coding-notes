#include "Graph.h"

void Graph::generateGraph(std::vector<std::vector<int>> &arr) {
	for (int i = 0; i < arr.size(); i++) {

	}
}

void Graph::doHandle(Vertex *ver) {
	std::cout << ver->val << std::endl;
}
